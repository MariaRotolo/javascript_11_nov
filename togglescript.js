const menuPhone = document.querySelector(".menu_list");
const button = document.querySelector(".button_menu_icon");


button.addEventListener("click", () => {
    menuPhone.classList.toggle("menu_list_phone");
});
    
        
   